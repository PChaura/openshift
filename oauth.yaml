apiVersion: config.openshift.io/v1
kind: OAuth
metadata:
  name: cluster
spec:
  identityProviders:
    - htpasswd:
        fileData:
          name: htpass-new
      mappingMethod: claim
      name: developer
      type: HTPasswd
  tokenConfig:
    accessTokenMaxAgeSeconds: 172800  