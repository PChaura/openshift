apiVersion: v1
kind: Service
metadata:
  name: mysite
spec:
  ports:
  - name: 80-tcp
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    deployment: mysite
  sessionAffinity: None
  type: ClusterIP